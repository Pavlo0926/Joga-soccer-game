using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class ActivePlayerStatminaValueAssign : MonoBehaviour
{
    /*
	public float forceData;
	public float timeData;
	public float aimData;
	public string diskName;
	public string statusData;
	public float idDisc;
	public float staminaData;

	public bool isSelectedDisc;

	public float player_timer_act;

	public static ActivePlayerStatminaValueAssign instance;

	void Awake ()
	{
		instance = this;
	}

	public void SelectDisc ()
	{
		for (int i = 0; i < SubStitutionManager.instance.InActiveDiscs.Count; i++) {
			if (SubStitutionManager.instance.InActiveDiscs [i].GetComponent <InActivePlayerStatminaValueAssign> ().isSelectedDisc) {

				for (int j = 0; j < SubStitutionManager.instance.ActiveDiscs.Count; j++) {
					SubStitutionManager.instance.ActiveDiscs [j].GetComponent <ActivePlayerStatminaValueAssign> ().DeSelectDisc ();
				}
				isSelectedDisc = true;
				SubStitutionManager.instance.SwipeButton.SetActive (true);
			}
		}
//		for (int i = 0; i < AssignFormationData.instance.InActiveDiscs.Count; i++) {
//			if (AssignFormationData.instance.InActiveDiscs [i].GetComponent <InActivePlayerStatminaValueAssign> ().isSelectedDisc) {
//
//				for (int j = 0; j < AssignFormationData.instance.ActiveDiscs.Count; j++) {
//					AssignFormationData.instance.ActiveDiscs [j].GetComponent <ActivePlayerStatminaValueAssign> ().DeSelectDisc ();
//				}
//				isSelectedDisc = true;
//				AssignFormationData.instance.SwipeButton.SetActive (true);
//			}
//		}
	}

	public void DeSelectDisc ()
	{
		SubStitutionManager.instance.SwipeButton.SetActive (false);
		isSelectedDisc = false;
	}
    */
}
